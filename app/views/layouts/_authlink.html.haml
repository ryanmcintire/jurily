%ul.nav.navbar-nav.navbar-right
  -if !current_user
    %li= link_to "Sign in", user_session_url
    %li= link_to "Sign up", new_user_registration_url
  -if current_user
    %li= link_to "Sign out", destroy_user_session_url, :method => :delete if current_user